{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [{
          "type": "command",
          "command": "if [ -f package.json ]; then npm run lint --silent 2>&1 | head -20; elif [ -f pyproject.toml ]; then ruff check --quiet 2>&1 | head -20; elif [ -f setup.py ] || [ -f requirements.txt ]; then ruff check --quiet 2>&1 | head -20; fi; exit 0"
        }]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [{
          "type": "command",
          "command": "echo \"$BASH_COMMAND\" | grep -qE '^(rm -rf|drop table|DELETE FROM|git push --force)' && echo 'BLOCKED: 危险操作被拦截，需人工确认' && exit 1 || exit 0"
        }]
      }
    ]
  }
}
