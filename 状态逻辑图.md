# 状态逻辑图 - Openclaw Claude Code Backend

## 系统架构概览

```
入口 server.js (thin wrapper)
  └─> src/server.js (Express app 初始化、中间件、路由挂载)
        ├─> src/routes/claude.js    — Claude CLI 交互（SSE 流式、同步调用）
        ├─> src/routes/session.js   — 会话 CRUD + 历史记录（JSONL 解析、分页）
        ├─> src/routes/tools.js     — 文件读写 + Bash 执行（白名单/黑名单安全策略）
        └─> src/routes/task.js      — Lead Agent 任务编排（CRUD + 执行 + SSE 状态流）
```

## 核心模块

| 模块 | 路径 | 职责 |
|------|------|------|
| Claude Runner | src/lib/claude-runner.js | 管理 Claude CLI 子进程生命周期、SSE 事件流、token 用量追踪 |
| Session Store | src/lib/session-store.js | 会话原子持久化（write-temp-rename）、schema 校验 |
| Logger | src/lib/logger.js | 结构化日志、LOG_LEVEL 分级 |
| Helpers | src/lib/helpers.js | 公共工具函数 |
| Lead Agent | src/lib/lead-agent.js | 任务分解、子任务编排、Claude CLI 多轮调用协调 |
| Task Store | src/lib/task-store.js | 任务持久化存储、状态查询、生命周期管理 |

## 请求流

### Claude 对话流（SSE）
```
Client POST /claude
  -> claude.js 校验参数
  -> claude-runner.js spawn CLI 子进程
  -> SSE stream: start -> content chunks -> heartbeat(30s) -> end/error
  -> 断开连接时 cleanup 子进程
```

### 会话管理
```
POST   /session          创建会话 -> session-store 原子写入
GET    /session/:id      读取会话
PUT    /session/:id      更新会话
DELETE /session/:id      删除会话
GET    /session/history   JSONL 解析 + 分页返回
```

### 工具执行
```
POST /tools/file-read    读取文件（路径校验）
POST /tools/file-write   写入文件（路径校验）
POST /tools/bash          执行命令（白名单/黑名单 + BASH_DISABLED 开关）
```

### Lead Agent 任务编排
```
POST   /task              创建任务 -> task-store 持久化
GET    /task/:id          查询任务状态
GET    /task              列出所有任务
POST   /task/:id/run      执行任务 -> lead-agent 分解子任务
  -> lead-agent.js 解析目标、生成执行计划
  -> 逐步调用 Claude CLI 执行子任务
  -> SSE stream: task-status 实时推送进度
  -> 完成/失败写回 task-store
DELETE /task/:id          删除任务
```

### Lead Agent 状态机
```
PENDING -> RUNNING -> COMPLETED
                  \-> FAILED
                  \-> CANCELLED
```

## 安全策略

- Bash 端点：命令白名单 + 黑名单双重过滤，BASH_DISABLED 环境变量可完全禁用
- 未实现端点返回 501（诚实响应，不伪装）
- 会话写入采用原子操作防止数据损坏

## 测试体系

- 75 个集成测试，使用 Node.js 内置 test runner
- CI: GitHub Actions 覆盖 Node 18/20/22
- 测试工具：fake-claude mock + inject-fake-claude 注入

## 配套文档

- OpenAPI 3.0 规范：docs/openapi.yaml
- MCP skill 扩展：openclaw-claude-code-skill/
